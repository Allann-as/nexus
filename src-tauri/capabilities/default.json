{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the main window. NEXUS talks to the outside world through its own commands only — no shell, no opener, no HTTP plugin. Grant nothing here without an ADR.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
